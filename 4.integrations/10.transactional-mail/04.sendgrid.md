---
reviewed: 2026-07-11
reviewer: fl
title: SendGrid
navigation.excerpt: Transactional mail integration
lead: SendGrid is a transactional email delivery service that helps reliably send emails from your fortrabbit app through SMTP or API integration.
figure:
  emoji: 📬
  text: Email delivery service integration.
  color: rgb(6, 182, 212)
  textColor: rgb(207, 250, 254)
section: Extending_fortrabbit
head:
  meta:
    - name: keywords
      content: sendgrid, transactional email, email delivery, fortrabbit, SMTP, API
---

::CallOut{alert}
Heads up. SendGrid was bought by Twilio some time ago. We don't plan to update this guide.
::

## About SendGrid

SendGrid is a transactional email delivery service that helps businesses reliably send and manage transactional emails through API and SMTP integration.

## Booking

To get started with SendGrid, you select a plan and create an account with a username, email, and password. Account provisioning includes human verification steps before your account is fully activated.

## Set up

To prevent your emails from reaching spam folders, you must verify domain ownership by configuring DNS records (DKIM and SPF) through your domain or DNS provider—fortrabbit is not involved in this step.

## Using SendGrid with fortrabbit

After domain verification is complete, integrate SendGrid into your fortrabbit app using the SendGrid PHP library via Composer. The **SendGrid PHP library** [here on GitHub](https://github.com/sendgrid/sendgrid-php) can be required like so:

```json [composer.json]
{
  "require": {
    "sendgrid/sendgrid": "~4.0"
  }
}
```

To send emails via the API, you'll need to specify your SendGrid API key. The [SendGrid PHP example on GitHub](https://github.com/sendgrid/sendgrid-php-example) demonstrates both API and SMTP gateway integration approaches.
