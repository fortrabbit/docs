---
reviewed: 2026-07-04
reviewer: fl
title: Outgoing firewall
navigation.excerpt: Making requests to the outer world from the environment
figure:
  emoji: ➡️
  text: Control outbound traffic rules.
  color: rgb(99, 70, 140)
  textColor: rgb(200, 210, 230)
lead: By default, outgoing (egress) traffic is limited on most non-standard ports for security reasons. Open ports from the dashboard to make outgoing calls from the app environment.
head:
  meta:
    - name: keywords
      content: 'outgoing firewall, egress traffic, firewall rules, environment settings, ports, fortrabbit'
---

:BlockLink{title="Outgoing firewall settings for {{app-env-id}}" path="/environments/{{app-env-id}}/settings/firewall-outgoing"}

Open ports with [environment](/1.platform/10.objects/02.environment.md) settings in the dashboard. Outgoing firewall rules are environment specific.

## Outgoing IPs

The outgoing firewall settings also show the outgoing IP for the environment.
