---
reviewed: 2026-07-04
reviewer: fl
title: Replace patterns
naviTitle: Replace patterns
navigation.excerpt: Define files and folders to be replaced
figure:
  emoji: 🔄
  text: Which files get replaced.
  color: rgb(30, 120, 160)
  textColor: rgb(207, 250, 254)
lead: A setting to define files and folders to be replaced during deployment with merge strategy.
head:
  meta:
    - name: keywords
      content: 'replace patterns, deployment, merge strategy, file synchronization, fortrabbit'
---

This is essential to avoid old files cluttering the system and confusing the software. This setting is only available when the [deployment strategy](/1.platform/05.deployment/08.strategy.md) is set to 'merge'. Note that with the merge strategy, all files from the deployment build will be synced into the content of the actual web storage. No old files will be deleted.

For certain folders, it's better to delete their content.

Sensible defaults based on [software templates](/1.platform/03.concepts/05.software-templates.md) are provided. Rsync syntax is supported, allowing regex-like patterns. The asterisk `*` serves as a wildcard.

## Examples

```yml
## Craft CMS
/config/project

## Pattern to replace all JPGs
*.jpg
```

## Related

When the [deployment strategy](/1.platform/05.deployment/08.strategy.md) is set to replace, there is an opposite setting to define [exclude patterns](/1.platform/05.deployment/10.exclude-patterns.md).
