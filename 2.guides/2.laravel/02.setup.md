---
rank: 30
reviewed: 2026-07-04
reviewer: fl
title: Laravel setup
naviTitle: Setup
navigation.excerpt: Configure your local installation
lead: Set up your Laravel application locally with environment variables and database configuration before deploying to fortrabbit.
figure:
  emoji: ⚙️
  hide: true
sidebar: laravel
nextNav: true
head:
  meta:
    - name: 'keywords'
      content: 'laravel, setup, configuration, environment variables, local development, mysql, deployment, fortrabbit'
---

## Get ready

Prepare your local environment with Laravel installed and Git configured, plus the fortrabbit GitHub App for automatic deployments.

- [Local development environment](/4.integrations/01.local-development/01.intro.md) up and running.
- [Laravel installed locally](/2.guides/2.laravel/01.install.md) best with attached Git repo.
- Install the [fortrabbit GitHub App](/1.platform/05.deployment/02.github-app.md) for automatic deployments.

## Create an app at fortrabbit

Create a new app in the fortrabbit dashboard. The wizard will guide you through the steps. You can connect the repo from GitHub.

:BlockLink{title="Create a new app" path="/new/app"}

## Software template

If Laravel is detected or you have chosen it in the software list, the Laravel [software template](/1.platform/03.concepts/05.software-templates.md) will be applied. This will pre-configure common ENV vars, build and post-deploy commands and set the root path to `public`.

## MySQL configuration

Not all Laravel projects require a database connection. When your app does, keep `config/database.php` as is when using the Laravel software template. The CAPITAL environment variable names (like `DB_HOST`, `DB_PASSWORD`) in `config/database.php` will be populated by fortrabbit automatically when you add a database. For local development, populate your `.env` file with your local database credentials. See the [ENV var topic](/3.dev/19.env-vars/01.intro.md) for more details.

This configuration enables your app to connect to the database.
